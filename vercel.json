{
    "builds": [
        {
            "src": "api/server/main server/server.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/server/main server/server.py"
        }
    ]
}
